# 旧版脚本归档

2026-09-11：桌面程序迁移时，将旧版五个 PowerShell 入口和两份 README 原样归档。`SHA256SUMS.txt` 记录移动前后的 SHA-256，已核对一致。

本目录用于历史参考。脚本内的 `$PSScriptRoot` 及相对资源路径仍按旧项目根目录设计；不要直接从本归档目录执行安装或更新。新版入口为发行包中的 `RenpyTranslator.exe`。

现役模组、字体和译文仍在项目根目录的 `game`、`fonts`、`translations` 中。旧版使用说明中的模型默认值和服务商信息是历史快照。
