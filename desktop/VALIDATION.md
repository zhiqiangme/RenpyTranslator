# 桌面版验证记录

## 更新页面与译文目录调整（2026-09-15）

- 仅较高的稳定版本显示下载与发布说明；同版本（含末尾 .0）、旧版本、乱序列表和缺少附件均有回归覆盖。检查失败时清除旧下载状态。
- 使用 Markdig 1.3.2 解析 Markdown，原生 WPF 文档显示标题、列表、强调、代码、表格与网页链接；许可证随发行包提供。同版本与模拟新版本两种更新页面已渲染检查。
- 应用级系统焦点模板置空，移除 Alt/键盘导航的默认黑色虚线框；保留控件原有焦点配色。空模板断言与键盘焦点页面预览通过。
- 78 个 JSONL 文件移至 `translations/camp-buddy-scoutmaster/`，移动前后 SHA-256 一致。安装、校验工具与资源打包路径同步调整；ZIP 中 78 个译文均位于专属目录，内容与源文件逐字节一致。
- Release 构建零警告、零错误；自包含 ZIP 构建成功，76 项管理器检查及 4 项更新器检查通过，ZIP SHA-256 核对通过。未运行真实游戏、未发布远端版本；本轮未编译安装器。

## 第二轮审查修复（2026-09-15）

- 管理器 Release 构建通过，零警告、零错误；68 项管理器自检通过。
- 损坏、空白、错误类型及缺少必要校验项的安装记录均返回可修复状态；有效配置与已识别的专属模式保留。重新安装后文件校验恢复正常，密钥保持不变。
- 跳过规则限定为共同支持的基础正则语法；测试覆盖内置规则与非捕获组，拒绝命名组、反向引用、扩展转义及不兼容次数限定。中英文 README 和界面说明同步更新。
- `python tools/test_translator_cache.py` 共 8 项测试通过：包含错误类型译文拒绝、有效译文接受、关闭时确认框与直接入队行为、发送前关闭、丢弃批次释放 pending 后重新入队，以及错误响应不进入缓存写入流程。
- API 响应由内存 mock 提供；工作循环确定性退出，不启动游戏、不调用真实模型。高级设置页 WPF 渲染成功；本轮未执行真实游戏、在线更新或安装包测试。

## 审查问题修复验证（2026-09-15）

- 管理器与更新器 Release 构建通过，零警告、零错误；默认管理器与资源版本均为 26.9.11。
- 最终管理器自检 50 项、更新器自检 4 项通过。新增覆盖尾随分隔符下的真实进程拦截、盘符根目录和相邻目录判断、损坏 JSON 的中文诊断、专属确认屏幕安装/切换/卸载、自定义字体保留、个人文件清理前备份，以及更新目录保留与占用保护。
- `python tools/test_translator_cache.py` 的 2 项测试通过：直接提取模组函数，验证归一化冲突保留首条兜底、精确变体仍独立保存、同源更新及空白键忽略；不执行 Ren'Py 初始化。
- 使用 `packaging/Publish.ps1 -Version 26.9.12 -OutputDirectory work/audit-release -SkipInstaller` 验证版本覆盖：自包含打包及当时的 48 项管理器检查、4 项更新器检查通过，发行资源版本与程序集一致，仓库默认版本未修改。随后补充的 2 项卸载检查已在最终开发构建通过。
- 游戏管理页 WPF 渲染检查通过，完整显示“删除全部文件（含自行存放的文件）”范围说明。
- 没有启动或修改真实游戏，没有调用真实模型，没有发布 Release；真实 Ren'Py 游戏内显示、在线更新与本轮 Inno Setup 安装包未实测。

以下为历史验证记录。

日期：2026-09-11；Windows x64；.NET SDK 10.0.301；管理器版本 1.0.0。

- 管理器与独立更新器 Release 构建成功，无编译错误或警告。
- 自包含发行包构建后执行 15 项管理器检查及 4 项更新器检查，全部通过。
- 覆盖 DPAPI 加解密、安装和资源哈希、通用模式隔离专属译文、保留缓存、注入故障后的原始字节恢复、完整译文合并、升级保留密钥、卸载保留用户数据、显式清理、重复译文拒绝及 ZIP 路径穿越拒绝。
- 本机模拟 HTTP 服务验证兼容接口请求、成功翻译响应、HTTP 401 及错误译文数量。不使用真实 API Key，不调用外部模型。
- 更新器在独立模拟发行目录验证替换程序、删除过期资源、保留无关文件，以及更新失败后恢复被修改和删除的文件。
- 五个 WPF 页面已渲染检查；API 页面服务商与配置值一致。高级参数页面使用滚动容器。
- 归档的五个脚本和两份旧 README 逐文件 SHA-256 一致。

遵循用户要求：没有启动游戏，也没有修改实际游戏安装。上述游戏目录均为 `%LOCALAPPDATA%/RenpyTranslator/tests` 下的模拟目录。游戏内显示效果、特定 Ren'Py 版本兼容性、真实模型 API 和线上 GitHub Release 更新尚未实测。

复现：运行 `packaging/Publish.ps1`。测试日志为 `%LOCALAPPDATA%/RenpyTranslator/self-test.log` 与 `updater-test.log`；测试目录和备份保留用于排查。

## 界面改版验证（2026-09-11）

范围：桌面管理器界面重做，零新增依赖，未引入任何第三方 UI 库。

改动：

- 新增 `Themes/Tokens.xaml`（颜色 / 圆角 / 字号令牌）、`Themes/Controls.xaml`（按钮、输入框、密码框、复选框、下拉框、滚动条、进度条、TabControl 八类控件的 ControlTemplate，均含常态 / 悬停 / 按下 / 禁用 / 键盘焦点五态）、`Themes/Icons.xaml`（线性矢量图标）。
- `MainWindow.xaml` 的顶部 TabControl 经模板重塑为左侧导航栏，页面由裸堆叠改为卡片分组；「高级设置」页控件由代码动态生成改为 XAML 声明，`MainWindow.xaml.cs` 只保留「配置键 → 控件」映射。
- 新增 `packaging/make-icon.py`（仅标准库）生成 `desktop/Translator/Assets/app.ico`，同一文件用于窗口图标与可执行文件图标。
- 修复缺陷：`--snapshot` 原先既不退出进程也不产出图片；现挂在 `ContentRendered` 上，逐页等待渲染管线完成，并写出 `snapshot.log` 记录每页结果。同时新增 `--snapshot <页索引>` 与 `--game <目录>`，可在载入真实游戏数据的状态下出图。
- 新增全局未处理异常处理：写入 `%LOCALAPPDATA%/RenpyTranslator/error.log` 并弹出提示，不再静默退出。

验证结果：

- `dotnet build -c Debug`：0 错误 0 警告。
- `packaging/Publish.ps1`：管理器与更新器 Release 单文件发布成功，两项自检全部通过。
- `--self-test`：15 项检查全部 PASS，运行于模拟目录 `%LOCALAPPDATA%/RenpyTranslator/tests`。
- 五个页面在「已安装 · 资源 26.8.11 · 文件完整」的真实数据状态下逐页渲染核对；截图可用 `RenpyTranslator.exe --snapshot [页索引]` 随时重新生成，不入库。
- 单文件发行包内嵌图标 7 种尺寸（16 ~ 256）逐一校验存在。

界面核对使用模拟目录 `%LOCALAPPDATA%/RenpyTranslator/ui-check`；未启动游戏，未修改实际游戏安装。

未做：游戏内实际显示效果、深色主题、`dist/` 发行包重新生成（仍为旧界面产物）。
